DATA SEGMENT
STR1 DB 'STUDY$'
DATA ENDS

EXT SEGMENT
STR2 DB 30 DUP(?)
EXT ENDS

CODE SEGMENT
MAIN PROC FAR
    ASSUME CS:CODE, DS:DATA, ES:EXT
START:
    MOV AX, DATA
    MOV DS, AX
    MOV AX, EXT
    MOV ES, AX
    LEA BX,STR1
    MOV DL, 0
    LOOP1:
    MOV AL,[BX]
    INC DL
    INC BX
    CMP AL,24H
    JNE LOOP1
    DEC DL
    ADD DL,30H
    MOV AH,02H
    INT 21H
    MOV AH,4CH
    INT 21H
    CODE ENDS
        END START
        


